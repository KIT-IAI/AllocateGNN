from SpatialAllocation.GNN.Layer.LossFunction.LossRegistry import LossRegistry

# Independent Stage 2 loss registry instance (separate from Stage 1's loss_registry)
allocation_loss_registry = LossRegistry()
